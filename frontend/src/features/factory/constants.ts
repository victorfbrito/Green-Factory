/** District fill colors by variant index. Used by DistrictLayer and FactoryDebugCard. */
export const DISTRICT_COLORS = ['#4a7c8c', '#5a8a7a', '#7a6a9a', '#8a7a5a'] as const

/** Block colors by district index. Must match ThreeWorldLayer baseColors. */
export const BLOCK_COLORS = ['#4f6bed', '#00b894', '#f39c12', '#9b59b6', '#e74c3c', '#16a085'] as const

/** Path road color (gray). */
export const PATH_COLOR = '#6b7280'

/** Service lane color (bright red). */
export const SERVICE_LANE_COLOR = '#ff4444'
